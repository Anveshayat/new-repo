trigger:
  branches:
    include:
      - '*'

pool:
  vmImage: ubuntu-latest 

variables:
  buildConfiguration: 'Release'
  Sonarorganization: 'anveshmyfirstapp'
  SonarConnection: 'sonarconnection'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: BuildJob
    displayName: 'Build and Test'
    steps:

    - task: UseDotNet@2
      displayName: 'Install .NET 8.0 SDK'
      inputs:
        packageType: 'sdk'
        version: '8.0.x'            # ✅ More specific version format

    - script: dotnet restore
      workingDirectory: 'AzureGitTest'
      displayName: 'Restore NuGet Packages'

    - task: SonarCloudPrepare@4
      displayName: 'Prepare SonarQube Analysis'
      inputs:
        SonarCloud: '$(SonarConnection)'
        organization: '$(Sonarorganization)'
        scannerMode: 'dotnet'
        projectKey: 'Anvesh_MyFirstApp'
        projectName: 'MyFirstApp'
        # ✅ Removed extraProperties completely - was causing issues

    - script: dotnet build --configuration $(buildConfiguration)
      workingDirectory: 'AzureGitTest'
      displayName: 'Compile Code'
    
    - script: dotnet test --configuration $(buildConfiguration)
      workingDirectory: 'AzureGitTest'
      displayName: 'Execute Unit Tests'

    - task: SonarCloudAnalyze@4
      displayName: 'Run SonarQube Analysis'
    
    - task: SonarCloudPublish@4
      displayName: 'Publish SonarQube Results'
      inputs:
        pollingTimeoutSec: '300'    

- stage: Publish
  displayName: 'Publish Stage'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: PublishJob
    displayName: 'Publish Artifacts'
    steps:
    - task: UseDotNet@2
      displayName: 'Install .NET 8.0 SDK'
      inputs:
        packageType: 'sdk'
        version: '8.0.x'            # ✅ Also needed in Publish stage

    - script: dotnet publish --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)
      workingDirectory: 'AzureGitTest'
      displayName: 'Publish Application'
    
    - task: ArchiveFiles@2
      displayName: 'Create Zip Package'
      inputs:
        rootFolderOrFile: '$(Build.ArtifactStagingDirectory)'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/app.zip'
        replaceExistingArchive: true
    
    - task: PublishBuildArtifacts@1
      displayName: 'Upload Artifacts'
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)/app.zip'
        ArtifactName: 'drop'